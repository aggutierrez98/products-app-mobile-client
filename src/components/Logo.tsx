import React from 'react';
import {LogoIcon, LogoContainer, LogoTitle} from '../theme/components/Logo';

export const Logo = () => {
  return (
    <LogoContainer>
      <LogoTitle>PRODUCTS APP</LogoTitle>
      <LogoIcon name="card-giftcard" />
    </LogoContainer>
  );
};
