import styled from 'styled-components/native';
import {EditButton} from '../categories/styles';

export const ActiveButton = styled(EditButton)`
  background-color: #05c105;
`;

export const ImageContainer = styled.TouchableOpacity`
  margin-top: 30px;
  margin-bottom: 20px;
  align-items: center;
  height: 250px;
  width: 100%;
  position: relative;
`;
